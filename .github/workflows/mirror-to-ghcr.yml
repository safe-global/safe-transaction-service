name: Mirror Docker Hub tags to GHCR

# One-off migration helper. Delete once the Docker Hub migration is signed off.
#
# Copies tags already published to Docker Hub into this repo's GHCR package
# without rebuilding, so each manifest digest is preserved exactly. Every
# production chain pins
# `v6.10.1@sha256:80db836cc5d5132d52b846ec2f4db5ba421ff1a8e56a6c3c20181da27962b1b1`,
# and a successful run must report that digest for the v6.10.1 tag.
#
# Pushing with the repository's GITHUB_TOKEN is what links the new package to
# this repo so it inherits repository access. Reads Docker Hub with the shared
# credential, so run this before that credential is revoked.

on:
  workflow_dispatch:
    inputs:
      tags:
        description: 'Space-separated tags to mirror'
        type: string
        default: >-
          staging develop latest v6.10.1 v6.10.0 v6.9.0 v6.8.0 v6.7.0
          v6.6.1 v6.6.0 v6.5.0 v6.4.0 v6.3.0 v6.2.2 v6.2.1 v6.1.0
          v6.0.3 v6.0.2 v6.0.1 v6.0.0 v5.42.1 v5.42.0 v5.41.0 v5.40.1
          v5.40.0 v5.39.3 v5.39.2 v5.39.1 v5.39.0 v5.38.0 v5.37.0 v5.36.0
          v5.35.0 v5.34.0 v5.33.1 v5.33.0 v5.32.0 v5.31.0 v5.30.1 v5.30.0
          v5.29.0 v5.28.1 v5.28.0 v5.27.0 v5.26.0 v5.25.2 v5.25.1 v5.25.0
          v5.24.1 v5.24.0 v5.23.1 v5.23.0 v5.22.0 v5.21.0 v5.20.0 v5.19.0
          v5.18.4 v5.18.3 v5.18.2 v5.18.1 v5.18.0 v5.17.0 v5.16.1 v5.16.0
          v5.15.0 v5.14.0 v5.13.1 v5.13.0 v5.12.0 v5.11.1 v5.11.0 v5.10.0
          v5.9.2 v5.9.1 v5.9.0 v5.8.2 v5.8.1 v5.8.0 v5.7.0 v5.6.0
          v5.5.0 v5.4.0 v5.3.1 v5.3.0 v5.2.2 v5.2.1 v5.2.0 v5.1.0
          v5.0.0 v4.37.2 v4.37.1 v4.37.0 v4.36.1 v4.36.0 v4.35.1 v4.35.0
          v4.34.0 v4.33.0 v4.32.2 v4.32.1 v4.32.0 v4.31.0 v4.30.0 v4.29.1
          v4.29.0 v4.28.4 v4.28.3 v4.28.2 v4.28.1 v4.28.0 v4.27.4 v4.27.3
          v4.27.2 v4.27.1 v4.27.0 v4.26.1 v4.26.0 v4.25.1 v4.25.0 v4.24.0
          v4.23.0 v4.22.4 v4.22.3 v4.22.2 v4.22.1 v4.22.0 v4.21.3 v4.21.2
          v4.21.1 v4.21.0 v4.20.4 v4.20.3 v4.20.2 v4.20.1 v4.20.0 v4.19.3
          v4.19.2 v4.19.1 v4.19.0 v4.18.6 v4.18.5 v4.18.4 v4.18.3 v4.18.2
          v4.18.0 v4.17.0 v4.16.0 v4.15.2 v4.15.1 v4.15.0 v4.14.2 v4.14.1
          v4.14.0 v4.13.2 v4.13.1 v4.13.0 v4.12.7 v4.12.4 v4.12.2 v4.12.1
          v4.12.0 v4.11.2 v4.10.2
      dry_run:
        description: 'Resolve and report digests without writing to GHCR'
        type: boolean
        default: true

permissions: {}

concurrency: ${{ github.workflow }}

env:
  SRC: docker.io/safeglobal/safe-transaction-service
  DST: ghcr.io/safe-global/safe-transaction-service
  CRANE_VERSION: v0.22.1
  CRANE_SHA256: 0ab7a1d6932a213aed964ce97666c3077fe691c8606413674a8b3e0b9ec4cda0

jobs:
  mirror:
    permissions:
      contents: read
      packages: write
    runs-on: ubuntu-latest
    steps:
      - name: Install crane (pinned, checksum verified)
        run: |
          set -euo pipefail
          curl -sSfL -o crane.tgz \
            "https://github.com/google/go-containerregistry/releases/download/${CRANE_VERSION}/go-containerregistry_Linux_x86_64.tar.gz"
          echo "${CRANE_SHA256}  crane.tgz" | sha256sum -c -
          tar -xzf crane.tgz crane
          sudo install -m 0755 crane /usr/local/bin/crane
          crane version

      - name: Log in to Docker Hub (source, read-only)
        env:
          DOCKER_USER: ${{ secrets.DOCKER_USER }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          set -euo pipefail
          printf '%s' "$DOCKER_PASSWORD" | crane auth login docker.io -u "$DOCKER_USER" --password-stdin

      - name: Log in to GHCR (destination)
        env:
          GH_ACTOR: ${{ github.actor }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          printf '%s' "$GH_TOKEN" | crane auth login ghcr.io -u "$GH_ACTOR" --password-stdin

      - name: Mirror tags
        env:
          INPUT_TAGS: ${{ inputs.tags }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail

          read -r -a TAGS <<< "$INPUT_TAGS"

          # GHCR throttles sustained blob writes with a 403, not a 429, which
          # crane treats as fatal. Serialising blob uploads stays under the
          # threshold; retrying the whole copy recovers if it is crossed anyway,
          # since blobs already uploaded are skipped.
          copy_with_retry() {
            local src="$1" dst="$2" attempt=1 max=4 backoff
            while :; do
              if crane copy -j 1 "$src" "$dst"; then
                return 0
              fi
              if [ "$attempt" -ge "$max" ]; then
                return 1
              fi
              backoff=$((attempt * 20))
              echo "  attempt $attempt failed - retrying in ${backoff}s"
              sleep "$backoff"
              attempt=$((attempt + 1))
            done
          }

          {
            echo "### \`$SRC\` → \`$DST\`"
            echo
            echo "| tag | digest | result |"
            echo "| --- | ------ | ------ |"
          } >> "$GITHUB_STEP_SUMMARY"

          failed=0

          for tag in "${TAGS[@]}"; do
            [ -n "$tag" ] || continue

            if ! src_digest=$(crane digest "$SRC:$tag" 2>/dev/null); then
              echo "FAIL  $tag - cannot read source manifest"
              echo "| \`$tag\` | | **failed** - unreadable source |" >> "$GITHUB_STEP_SUMMARY"
              failed=$((failed + 1))
              continue
            fi

            if [ "$DRY_RUN" = "true" ]; then
              echo "would copy  $tag  $src_digest"
              echo "| \`$tag\` | \`$src_digest\` | dry run |" >> "$GITHUB_STEP_SUMMARY"
              continue
            fi

            if ! copy_with_retry "$SRC:$tag" "$DST:$tag"; then
              echo "FAIL  $tag - copy did not complete after retries"
              echo "| \`$tag\` | \`$src_digest\` | **failed** - copy error |" >> "$GITHUB_STEP_SUMMARY"
              failed=$((failed + 1))
              continue
            fi

            dst_digest=$(crane digest "$DST:$tag")

            if [ "$src_digest" != "$dst_digest" ]; then
              echo "DIGEST MISMATCH on $tag"
              echo "  src $src_digest"
              echo "  dst $dst_digest"
              echo "| \`$tag\` | \`$src_digest\` | **DIGEST MISMATCH** |" >> "$GITHUB_STEP_SUMMARY"
              failed=$((failed + 1))
              continue
            fi

            echo "ok    $tag  $src_digest"
            echo "| \`$tag\` | \`$src_digest\` | copied, digest preserved |" >> "$GITHUB_STEP_SUMMARY"
          done

          if [ "$failed" -gt 0 ]; then
            echo "::error::$failed tag(s) failed to mirror"
            exit 1
          fi
